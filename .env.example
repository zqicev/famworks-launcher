# Скопируй этот файл в .env и вставь свой токен.
# Токен GitHub для публикации релизов (npm run release).
# Права: classic — public_repo, либо fine-grained — Contents: Read and write на famworks-builds.
GH_TOKEN=
